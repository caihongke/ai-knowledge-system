# Step 5 执行计划：AI自学知识管理系统

> 生成时间：2026-03-21
> 执行周期：2026-03-22 ~ 2026-04-04（14天）
> 每日投入：2-3 小时
> 状态：执行中

---

## 🚀 启动引导：你的第一步

> **现在就做这一件事：**
> 打开终端，运行以下命令初始化项目：
> ```bash
> cd ~/AI-Platform
> pip install typer rich
> mkdir -p cli core knowledge/notes
> touch cli/__init__.py cli/main.py core/__init__.py core/config.py
> ```
> 完成后你就有了项目骨架，第一步就这么简单。

---

## 一、14天每日待办清单

### 📅 Day 1（03-22）WP0 基础设施 - 上半

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D1.1 | 安装依赖：`pip install typer rich` | 5min | import typer 不报错 |
| D1.2 | 创建目录结构：cli/, core/, knowledge/notes/ | 10min | 目录存在 |
| D1.3 | 编写 `cli/main.py` CLI 入口（Typer app 骨架） | 30min | `python -m cli.main --help` 正常输出 |
| D1.4 | 编写 `core/config.py` 配置管理（路径常量） | 30min | `from core.config import Config` 可用 |

**今日目标**：CLI 能跑起来，`--help` 有输出。

---

### 📅 Day 2（03-23）WP0 基础设施 - 下半

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D2.1 | 定义笔记数据模型（Note dataclass/dict） | 20min | 字段包含 id, title, tags, category, created_at, updated_at |
| D2.2 | 创建 `knowledge/index.json` 初始索引文件 | 10min | 空列表 JSON 文件存在 |
| D2.3 | 编写 `core/storage.py` 存储层（load/save index） | 40min | 读写 index.json 单元测试通过 |
| D2.4 | 注册子命令组：`kb`, `review`, `learn`, `sync` | 20min | 各子命令 `--help` 可用 |

**今日目标**：🏁 **里程碑 M1 达成** — 基础设施就绪。

---

### 📅 Day 3（03-24）WP1 知识库 - 笔记录入

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D3.1 | 实现 `kb add "标题" --tags "a,b"` 命令 | 40min | 创建 .md 文件 + 更新索引 |
| D3.2 | 自动生成笔记 ID（时间戳或递增） | 15min | ID 唯一且可排序 |
| D3.3 | 实现 `kb edit <id>` 打开编辑器修改笔记 | 30min | 调用系统默认编辑器 |

**今日目标**：能新建和编辑笔记。

---

### 📅 Day 4（03-25）WP1 知识库 - 分类与标签

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D4.1 | 实现 `kb list` 展示所有笔记列表 | 30min | 终端表格显示 ID/标题/标签/时间 |
| D4.2 | 实现 `--tag` 和 `--category` 过滤参数 | 30min | `kb list --tag python` 正确筛选 |
| D4.3 | 实现 `kb tags` 展示所有标签及计数 | 20min | 输出标签云列表 |

**今日目标**：笔记可按标签/分类筛选。

---

### 📅 Day 5（03-26）WP1 知识库 - 检索与查看

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D5.1 | 实现 `kb search "关键词"` 全文检索 | 40min | 搜索标题 + 内容，高亮匹配 |
| D5.2 | 实现 `kb show <id>` 查看笔记详情 | 30min | 用 rich 渲染 Markdown |
| D5.3 | 实现 `kb delete <id>` 删除笔记（带确认） | 20min | 删除文件 + 更新索引 |

**今日目标**：知识库 CRUD 全部完成。

---

### 📅 Day 6（03-27）WP1 知识库 - 完善与测试

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D6.1 | 端到端测试：完整走一遍 add→list→search→show→delete | 30min | 全流程无报错 |
| D6.2 | 修复测试中发现的 Bug | 30min | 所有命令稳定可用 |
| D6.3 | 用 `kb add` 录入 3 条真实学习笔记（吃自己的狗粮） | 30min | 知识库有真实内容 |

**今日目标**：🏁 **里程碑 M2 达成** — 知识库功能完整可用。奖励：解锁 1 门课程章节！

---

### 📅 Day 7（03-28）WP3 间隔复习 - 算法

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D7.1 | 编写 `core/spaced_repetition.py` 艾宾浩斯算法 | 40min | 输入复习次数，输出下次间隔天数 |
| D7.2 | 创建 `knowledge/review_schedule.json` 数据文件 | 10min | 初始空结构 |
| D7.3 | 实现：新建笔记时自动注册复习计划 | 30min | `kb add` 后 review_schedule 自动更新 |

**今日目标**：复习算法就绪，新笔记自动纳入复习。

---

### 📅 Day 8（03-29）WP3 间隔复习 - 命令

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D8.1 | 实现 `review today` 列出今日待复习笔记 | 40min | 按到期日期筛选，表格展示 |
| D8.2 | 实现 `review done <id>` 标记完成 | 30min | 更新复习次数 + 计算下次日期 |
| D8.3 | 实现 `review stats` 显示复习统计 | 20min | 展示总数/今日/已掌握数 |

**今日目标**：复习命令可用。

---

### 📅 Day 9（03-30）WP3 间隔复习 - 测试

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D9.1 | 为已有 3 条笔记补充复习计划 | 15min | 旧笔记也纳入复习 |
| D9.2 | 端到端测试复习流程 | 30min | today→show→done 全流程通过 |
| D9.3 | 修复 Bug + 边界情况处理 | 30min | 无笔记时友好提示 |

**今日目标**：🏁 **里程碑 M3（调整）达成** — 复习系统可用。奖励：购买 1 本技术电子书！

---

### 📅 Day 10（03-31）WP2 飞书同步 - 凭证与封装

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D10.1 | 登录飞书开放平台，获取/刷新 APP_SECRET | 30min | `.env` 中凭证有效 |
| D10.2 | 测试 `feishu_final.py` 能否成功上传 | 15min | 上传测试文件成功 |
| D10.3 | 编写 `cli/sync.py`，封装 `sync feishu` 命令 | 40min | CLI 可调用同步 |

> 若 D10.1 失败：切换 Plan B，实现 `sync export` 本地导出命令。

**今日目标**：飞书同步或本地导出可用。

---

### 📅 Day 11（04-01）WP2 飞书同步 - 增量同步

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D11.1 | 在 index.json 添加 `last_synced` 字段 | 20min | 字段存在 |
| D11.2 | 实现增量同步逻辑（仅上传变更笔记） | 40min | 二次同步跳过未变更 |
| D11.3 | 端到端测试同步流程 | 20min | 新增→同步→再同步 通过 |

**今日目标**：🏁 **里程碑 M4 达成** — 飞书同步可用。奖励：解锁新工具/插件！

---

### 📅 Day 12（04-02）WP4 AI推荐 - API接入

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D12.1 | `pip install anthropic`，配置 API Key 到 `.env` | 15min | SDK 可导入 |
| D12.2 | 编写 `core/ai_engine.py` 封装 Claude 调用 | 45min | 发送 prompt 返回响应 |
| D12.3 | 实现调用次数统计和月度上限检查 | 20min | 超限时输出警告 |

**今日目标**：AI 引擎可调用。

---

### 📅 Day 13（04-03）WP4 AI推荐 - 分析与建议

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D13.1 | 实现知识库摘要生成（提取标题+标签列表） | 30min | 输出当前知识领域概览 |
| D13.2 | 编写学习建议 Prompt 模板 | 20min | Prompt 结构化，含角色/上下文/任务 |
| D13.3 | 实现 `learn suggest` 命令 | 30min | 输出 AI 生成的学习路径建议 |

**今日目标**：AI 推荐功能可用。

---

### 📅 Day 14（04-04）收尾与交付

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D14.1 | 全功能端到端测试 | 40min | kb/review/sync/learn 全部通过 |
| D14.2 | 编写 README.md 使用说明 | 30min | 包含安装和命令示例 |
| D14.3 | 缓存 AI 建议结果（24h 有效） | 20min | 重复查询不调 API |

**今日目标**：🏁 **里程碑 M5 达成** — MVP 完整交付！奖励：半天自由探索时间！

---

## 二、进度追踪看板

> 每日更新此看板，将任务 ID 移动到对应列。

### 当前状态（03-21 初始化）

| 待办 (To Do) | 进行中 (Doing) | 已完成 (Done) |
|:-------------|:---------------|:--------------|
| D1.1 安装依赖 | | |
| D1.2 创建目录 | | |
| D1.3 CLI入口 | | |
| D1.4 配置管理 | | |
| D2.1 数据模型 | | |
| D2.2 索引文件 | | |
| D2.3 存储层 | | |
| D2.4 注册子命令 | | |
| D3.1~D14.3 ... | | |

### 里程碑追踪

| 里程碑 | 计划日期 | 实际日期 | 状态 | 奖励 |
|--------|----------|----------|------|------|
| M1 基础设施就绪 | 03-23 | — | 🔲 待完成 | 解锁技术文章 |
| M2 知识库可用 | 03-27 | — | 🔲 待完成 | 解锁课程章节 |
| M3 复习系统可用 | 03-30 | — | 🔲 待完成 | 技术电子书 |
| M4 飞书同步可用 | 04-01 | — | 🔲 待完成 | 新工具/插件 |
| M5 MVP交付 | 04-04 | — | 🔲 待完成 | 半天自由探索 |

### 燃尽统计

| 日期 | 完成任务数 | 累计完成 | 剩余 | 备注 |
|------|-----------|---------|------|------|
| 03-22 | — | 0 | 42 | Day 1 |
| 03-23 | — | — | — | |
| ... | — | — | — | |

---

## 三、每日跟踪提醒

每天开发结束后，请执行：
1. 更新看板：将完成的任务移到「已完成」列
2. 记录进展：`python -m cli.main kb add "Day X 进展" --tags "日志,进度"`
3. 如遇阻塞：立即记录问题，评估是否需要启动 Plan B

---

## 四、快速参考卡

```
项目命令速查：
  python -m cli.main kb add "标题" --tags "标签"    # 新建笔记
  python -m cli.main kb list                        # 列出笔记
  python -m cli.main kb list --tag python            # 按标签筛选
  python -m cli.main kb search "关键词"              # 全文检索
  python -m cli.main kb show <id>                    # 查看笔记
  python -m cli.main kb delete <id>                  # 删除笔记
  python -m cli.main review today                    # 今日复习
  python -m cli.main review done <id>                # 完成复习
  python -m cli.main sync feishu                     # 同步飞书
  python -m cli.main learn suggest                   # AI学习建议
```

---

*本执行计划由 Step 5 流程生成。开发过程中请使用 `/step6-monitor` 汇报进度。*
