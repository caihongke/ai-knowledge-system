# Step5-执行看板

# Step 5 执行计划：AI自学知识管理系统

> 生成时间：2026-03-21
> 执行周期：2026-03-22 ~ 2026-04-04（14天）
> 每日投入：2-3 小时
> 状态：执行中

---

## 🚀 启动引导：你的第一步

> **现在就做这一件事：**
> 打开终端，运行以下命令初始化项目：
> ```bash
> cd ~/AI-Platform
> pip install typer rich
> mkdir -p cli core knowledge/notes
> touch cli/__init__.py cli/main.py core/__init__.py core/config.py
> ```
> 完成后你就有了项目骨架，第一步就这么简单。

---

## 一、14天每日待办清单

### 📅 Day 1（03-22）WP0 基础设施 - 上半

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D1.1 | 安装依赖：`pip install typer rich` | 5min | import typer 不报错 |
| D1.2 | 创建目录结构：cli/, core/, knowledge/notes/ | 10min | 目录存在 |
| D1.3 | 编写 `cli/main.py` CLI 入口（Typer app 骨架） | 30min | `python -m cli.main --help` 正常输出 |
| D1.4 | 编写 `core/config.py` 配置管理（路径常量） | 30min | `from core.config import Config` 可用 |

**今日目标**：CLI 能跑起来，`--help` 有输出。

---

### 📅 Day 2（03-23）WP0 基础设施 - 下半

| # | 待办 | 预估 | 验收标准 |
|---|------|------|----------|
| D2.1 | 定义笔记数据模型（Note dataclass/dict） | 20min | 字段包含 id, title, tags, category, created_at, updated_at |
| D2.2 | 创建 `knowledge/index.json` 初始索引文件 | 10min | 空列表 JSON 文件存在 |
| D2.3 | 编写 `core/storage.py` 存储层（load/save index） | 40min | 读写 index.json 单元测试通过 |
| D2.4 | 注册子命令组：`kb`, `review`, `learn`, `sync` | 20min | 各子命令 `--help` 可用 |

**今日目标**：🏁 **里程碑 M1 达成** — 基础设施就绪。

---
